name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build
      uses: SAP/project-piper-action@main
      with:
        step-name: mtaBuild
        flags: '--buildTarget=CF'